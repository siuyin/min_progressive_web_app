# Minimal Progressive Web App
Just enough code to trigger an install on Chrome.

## tempconv: Celcius to Farenheit converter
To make a progressive web app.  
First make a web app.  
Then add a service worker to cache assets so that it works offline.


